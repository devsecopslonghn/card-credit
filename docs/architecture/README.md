# Architecture

This directory contains documentation describing the current system
architecture and runtime boundaries.

Architecture documents should describe implemented behavior, not planned
behavior. Proposed changes belong in `docs/plans/active/` or
`docs/decisions/`.