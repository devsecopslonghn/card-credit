export type MonthlyCashFlow = { cardId: string; period: string; totalOut: number; totalIn: number; statementPayments: number; actualFees: number; partnerReturns: number; bankCashbackActual: number; netResult: number; card?: { providerName?: string; displayName?: string; bank?: string; name?: string; owner?: string } | null };
export const fetchMonthlyCashFlow = async (period: string) => { const response = await fetch(`/api/cash-flow/monthly?period=${encodeURIComponent(period)}`, { cache: "no-store" }); if (!response.ok) throw new Error("Không thể tải tổng quan dòng tiền."); return ((await response.json()) as { data?: MonthlyCashFlow[] }).data ?? []; };
