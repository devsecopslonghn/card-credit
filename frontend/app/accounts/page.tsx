"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { FinanceShell, vnd, typeClass, typeLabel } from "@/components/finance/FinanceShell";
import { listFinanceAccounts, type FinanceAccount } from "@/lib/api/financeClient";
export default function AccountsPage() { const [accounts, setAccounts] = useState<FinanceAccount[]>([]); const [error, setError] = useState(""); useEffect(() => { void listFinanceAccounts().then(setAccounts).catch(() => setError("Không thể tải tài khoản.")); }, []); const real = useMemo(() => accounts.filter((a) => a.group === "REAL_MONEY"), [accounts]); const debt = useMemo(() => accounts.filter((a) => a.group === "DEBT"), [accounts]); const group = (items: FinanceAccount[], title: string, tone: string) => <section className="cc-section p-5"><div className="mb-4 flex items-center justify-between"><h2 className={`text-xl font-bold ${tone}`}>{title}</h2><span className="text-sm cc-text-muted">{items.length} tài khoản</span></div>{items.length ? <div className="grid gap-4 md:grid-cols-2">{items.map((account) => <article key={account.id} className="rounded-xl border p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{account.name}</h3><p className="mt-1 text-sm cc-text-muted">{typeLabel[account.type]}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${typeClass[account.type]}`}>{account.type}</span></div><p className="mt-5 text-xl font-bold">{vnd(account.openingBalance)}</p><p className="mt-1 text-xs cc-text-muted">Số dư ban đầu · {account.currency}</p></article>)}</div> : <p className="py-8 text-center cc-text-muted">Chưa có tài khoản.</p>}</section>; return <FinanceShell title="Tài khoản" action={<Link href="/transactions?add=1" className="rounded-lg bg-[#087f83] px-4 py-2.5 font-semibold text-white">+ Thêm giao dịch</Link>}>{error ? <p role="alert" className="mb-4 rounded-lg bg-red-50 p-4 text-red-700">{error}</p> : null}<div className="space-y-6">{group(real, "Tiền thực tế", "text-cyan-700")}{group(debt, "Tài khoản tín dụng", "text-violet-700")}</div></FinanceShell>; }
