"use client";
import { useState } from "react";
export function AiTransactionModal({ onClose }: { onClose: () => void }) { const [text, setText] = useState(""); const [preview, setPreview] = useState(false); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="ai-title"><div className="w-full max-w-4xl rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 id="ai-title" className="text-2xl font-bold">Thêm giao dịch bằng AI</h2><button onClick={onClose} className="rounded-lg px-3 py-2 text-xl" aria-label="Đóng">×</button></div><div className="mt-6 grid gap-5 md:grid-cols-2"><div><label htmlFor="ai-input" className="text-sm font-semibold">Mô tả giao dịch</label><textarea id="ai-input" value={text} onChange={(event) => setText(event.target.value)} className="cc-control mt-2 min-h-64 w-full rounded-xl p-4" placeholder="Ví dụ: Hôm nay ăn sáng 45k, cà phê 25k bằng Vietcombank..."/><button disabled={!text.trim()} onClick={() => setPreview(true)} className="mt-3 w-full rounded-lg bg-[#087f83] px-4 py-3 font-bold text-white disabled:opacity-50">Tạo bản xem trước</button></div><div className="rounded-xl bg-slate-50 p-5"><h3 className="font-bold">Bản xem trước</h3>{preview ? <><p className="mt-4 text-sm">Nội dung đã sẵn sàng để phân tích:</p><p className="mt-2 rounded-lg bg-white p-3 text-sm">{text}</p><p className="mt-4 text-xs text-slate-600">AI chỉ phân tích câu nói. Backend sẽ tính toán và yêu cầu xác nhận trước khi ghi dữ liệu.</p><div className="mt-5 flex gap-3"><button onClick={onClose} className="rounded-lg bg-[#087f83] px-4 py-2 font-semibold text-white">Mở preview MCP</button><button onClick={() => setPreview(false)} className="rounded-lg border px-4 py-2 font-semibold">Chỉnh sửa</button></div></> : <p className="mt-8 text-sm text-slate-500">Nhập câu mô tả để tạo preview. Chưa có dữ liệu nào được ghi.</p>}</div></div></div></div>; }
