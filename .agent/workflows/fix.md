# Fix workflow

Reproduce the failure, locate the root cause, make the minimal fix, add or
update a regression test, run all relevant existing checks, and record the
evidence. Allow up to three normal repair attempts before escalation.
