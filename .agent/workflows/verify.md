# Verify workflow

Inspect the diff, select the relevant existing gates, run
`.agent/gates/verify.sh` (or `full`), inspect failures, and report PASS, FAIL,
BLOCKED or HUMAN_DECISION from command evidence. Agent prose cannot override a
failed command.
